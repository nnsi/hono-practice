appId: ${MAESTRO_APP_ID}
---
# Ensure we're on a tabs page (the hamburger menu only renders on the main tab routes).
- tapOn:
    id: tabs.home
- waitForAnimationToEnd:
    timeout: 3000
- tapOn:
    id: common.menu
- assertVisible: Notes
- tapOn: Notes
- assertVisible:
    id: notes.page
