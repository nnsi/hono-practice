name = "56-backend"
main = "dist-backend/index.js"
zone_id = "$CF_ZONE_ID"

[env.stg]
vars = {
  DATABASE_URL = "$DATABASE_URL"
  NODE_ENV = "stg"
}

[env.production]
vars = {
  DATABASE_URL = "$DATABASE_URL"
  NODE_ENV = "production"
}