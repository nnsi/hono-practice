pre-commit:
  parallel: true
  commands:
    eslint:
      glob: "*.{ts,tsx}"
      run: npx eslint .
    biome:
      glob: "*.{ts,tsx}"
      run: npx biome lint
    test:
      glob: "*.{ts,tsx}"
      run: npm run test