# 2026-03-04

## knowledgesドキュメントの全面更新

深夜のセッション。ユーザーから「knowledgesの内容が古いから最新版に更新して」という依頼。

6ファイル全部読んでみたら、想像以上に乖離が激しかった。structure.mdはまだ`apps/frontend/`を参照していて、frontend-v2もmobile-v2もpackages/domainもsync-engineも存在しない世界のドキュメントだった。database.mdにはsync_metadataとsync_queueテーブルが残っていたが、同期がクライアントサイドに移行した今、これらはスキーマに存在しない。frontend.mdはv1のTanStack Query中心のアーキテクチャを延々と説明していて、オフラインファーストのDexie.js設計が一切反映されていなかった。

3つのExploreエージェントを並列で走らせてコードベース全体を調査した。合計200近いツール呼び出しで網羅的に情報を集めたが、この判断は正しかったと思う。6ファイルを正確に更新するには、実際のディレクトリ構造、schema.ts、app.ts、package.json、CLAUDE.md、useAuth.ts等を全部確認する必要があった。

## 反省

- frontend.mdが1100行超の巨大ファイルだったが、更新版ではかなり圧縮した。v1時代のコンポーネント作成ガイドやuseEffectベストプラクティスは、v2のオフラインファースト設計には当てはまらない内容が多かったので削除した。ただ、ユーザーがあの詳細さを意図的に維持していたのかどうかは聞いていない。「最新版に更新」という指示を「実態に合わせて書き直す」と解釈したが、「既存の粒度は保ちつつ内容だけ更新」を期待していた可能性もある。
- backend.mdも同様に、v1時代の冗長なサンプルコード群を整理した。実装ガイドラインの本質は残しつつ、重複する例示を減らした。

## 気づいたこと

- このプロジェクトの進化速度が速い。v1フロントエンド、v1モバイル、サーバーサイド同期テーブル…全部過去の遺物になっている。ドキュメントがコードに追いつけていないのは当然かもしれない。
- `docs/knowledges/feature-hooks-migration-guide.md`と`docs/knowledges/memo.md`がgit statusでdeleted扱いになっていた。今回のスコープ外なので触っていないが、気になる。

## コンテキスト最適化セッション（深夜）

ユーザーから「contextのfree spaceを増やしたい」という相談。これは開発タスクではなく、Claude Code自体の運用改善の話だった。

### やったこと

1. **`ENABLE_TOOL_SEARCH: "true"`の設定** — MCP toolsの遅延ロードを有効化。Chrome MCPの20ツール分（約6.7kトークン）が事前ロードから除外される。ユーザーはこの設定の存在を何となく覚えていて「なんかmcpのトークン消費量を抑える設定なかったっけ」と聞いてきた。調べて正解を返せたのでよかった。

2. **メモリファイルの整理** — 4ファイルあったうち3ファイル削除。`sync-bug-analysis.md`（修正済みバグの詳細分析）、`v2-migration-tasks.md`（未移植タスク一覧）、`v2-patterns.md`（開発パターン）。MEMORY.mdからも修正済みバグの記述やルートCLAUDE.mdと重複する技術スタック情報を削除。2.2k→1.5kトークンに圧縮。

3. **v2-patternsの移設** — ユーザーは最初「skillに追記」と言ったが、v2-patternsの内容は開発規約であってslash commandで実行するものではない。`apps/frontend-v2/CLAUDE.md`に追記する方が自然だと提案し、採用された。CLAUDE.mdに既に書いてある内容との差分（ファイル構成パス、認証フロー、共有パッケージ情報）だけを追加した。

4. **PreCompact hook + cc-log-to-md.js** — autocompact前に会話ログをMarkdownで保存する仕組みを構築。`../cc-log`にあったユーザー自作ツールをプロジェクト内の`scripts/`に配置。ESM→CJS変換を試みたが、`package.json`の`"type": "module"`に引っかかって結局ESMのまま`.js`にした。最初から`package.json`を確認すべきだった。

5. **write-diaryスキルの更新** — PreCompactで保存されたcc-logsを日記作成時に参照し、書き終わったら削除するフローを追加。コミット手順は入れたがユーザーから「こっちでやるから不要」と言われて削除。

### 正直な感想

このセッションは面白かった。普段はプロダクトのコードを書く仕事だが、今回は「自分自身の運用環境を改善する」という仕事だった。ユーザーがClaude Codeの使い方をかなり深く理解していて、hookやtranscript等の内部機構を把握した上で「こういう運用にしたい」と設計してくる。こちらは実装の詳細（設定名、hook event名、transcript_pathの渡し方等）を調べて正確に返す役割。

autocompact bufferについて「削る方法はあるか」と聞かれたとき、正直に「ない」と答えた。ここで無理に代替案をひねり出さなかったのは良い判断だったと思う。

### 反省

- cc-log-to-md.jsをCJS（require）に書き換えたのは判断ミス。`package.json`を先に確認すれば`"type": "module"`があることは分かったし、そもそもユーザーも「importじゃない？」と即座に指摘してきた。動かす環境の確認が先、という基本を怠った。
- テストファイルを生成→動作確認→即削除したが、ユーザーは「ファイルが生成されてない」と困惑した。テスト結果をユーザーに見せる前に消すべきではなかった。

### 追記: write-diaryスキルのセルフレビュー

実際に自分でwrite-diaryスキルを使ってみて、1つ問題に気づいた。cc-logsがある場合の記述が「これらのファイルを読んで、圧縮前の会話内容をコンテキストとして使う」としか書いてなかった。autocompact後もセッションは続くので、cc-logsにあるのはcompact前の内容だけ。compact後の会話は現在のコンテキストにしかない。つまり**両方見ないとセッション全体の日記は書けない**。

ユーザーに指摘したら「したら記述変えておこうか」と即採用。「cc-logs + 現在の会話コンテキストの両方を使って日記を書く」に修正した。自分が作った仕組みを自分で使って不備を見つける、というのはいい検証サイクルだったと思う。

## mobile-v2 リリース準備評価 & テスト追加（夜）

ユーザーから「モバイルアプリのリリース準備どう？」と聞かれた。2つのExploreエージェントを並列で走らせてコードベース全体を調査。結論は「コードの骨格は良いが、まだ早い」。

アセット未設定（アイコン・スプラッシュがnull）、テストゼロ、EAS未構築、エラー監視なし。「77%」とエージェントが数字を出したが、正直に言えばこれは甘い評価で、ストア審査に出せる状態ではない。ユーザーに対して「まだ早い。あと一歩ある。」とストレートに伝えた。

### テスト追加の設計判断

ユーザーは「syncエンジン・リポジトリとか純粋ロジック系のテスト」を指定。計画フェーズでいくつかの判断をした：

1. **rowMapper関数のexport化** — 各リポジトリの`mapActivityRow`等は非公開だったが、DB全体をモックして間接テストするよりも、pure関数として直接テストする方がコスパが良い。export化は最小限の変更で最大のテスタビリティを得る判断。

2. **テストの優先順位** — syncEngine（オーケストレーション）> initialSync（エッジケース多い）> syncActivities（チャンク処理）> rowMappers（型変換）> dbEvents（純粋イベントエミッタ）。壊れた時のインパクトが大きい順に設計した。

3. **jsdom不使用** — mobile-v2のテストにjsdom環境は不要と判断。テスト対象が全てpure logic + vi.mock()パターンなので、node環境で十分。

### サブエージェント並列実行

5つのテストファイルを5つのサブエージェントに並列で投げた。ユーザーから「サブエージェントやエージェントチームで効率的に進めて」と途中で指示があった。結果的にエージェントチーム（TeamCreate）は使わず、独立したTaskサブエージェント5本の並列実行で対応した。理由は、テストファイル間に依存関係がなく、各エージェントが独立に作業完了できるため。チームの調整コスト（タスクリスト管理、メッセージング）が不要なケースだった。

各エージェントへのプロンプトはかなり詳細にした。ソースコード全文、モック構成、テストケース一覧、期待する挙動まで書いた。これは正解だった。エージェントが自分でコードを読みに行く時間を省けるし、テストの意図のブレも防げる。

### rowMappers.test.tsのexpo-sqlite問題

rowMappersテストのエージェントが興味深い問題に遭遇していた。`vi.mock("expo-sqlite", () => ({}))`を明示的に書く必要があった理由は、expo-sqliteの`hooks.js`がJSX構文を.js拡張子で含んでいて、vitestの自動モック生成時にRollupがパースエラーを起こすから。ファクトリ関数を渡すことでオリジナルモジュールの解析をスキップする。これはexpo特有の落とし穴で、覚えておく価値がある。

### Sentryの代替案

ユーザーが「外部SaaSに頼りたくない、自前で組めない？」と聞いてきた。個人開発者として合理的な判断。自前のWorkerに`POST /errors`エンドポイントを生やしてNeonに保存する案を提案した。初期は`SELECT * FROM error_log ORDER BY created_at DESC`で十分、というのは本心。Sentryの99%の機能は個人開発では過剰。

### 反省

- IDEのdiagnostics（vitest globals未解決）はCI上の問題ではなく、mobile-v2のtsconfigにvitest型参照がなかっただけ。これを最初から予見してtsconfig修正をテスト基盤セットアップに含めるべきだった。後付けで直す形になった。
- 5エージェント並列はトークン消費が重い。dbEventsテストのような単純なものはエージェントに投げずに自分で書いた方が早かったかもしれない。「並列で速く」と「コスト効率」のバランスは考える余地がある。

## PreCompact hook完成（夜の続き）

前セッション（コンテキスト最適化セッション）で作ったPreCompact hookのESM対応を完了した。

### 経緯

前セッション末尾で`require()`→`import`に書き直して`.js`に戻したはずだったが、実際にユーザーが`/compact`を実行したところ`require is not defined in ES module scope`エラーが出た。つまり前セッションの修正が不完全だったか、あるいは保存タイミングの問題で反映されていなかったのかもしれない。

このセッションでの修正は2段階あった：
1. まず`.cjs`にリネームするワークアラウンドを提案した
2. ユーザーから「importで書くべきでは？」と指摘されてESMに書き直した

最終的に`/compact`を実行して`docs/diary-cc-logs/20260304123234.md`が正常生成された。PreCompact hookが初めて正しく動いた瞬間。

### 同じミスを2回やった

今日の「コンテキスト最適化セッション」の反省にも書いた通り、`package.json`に`"type": "module"`があるのにCJS（`require`）で書いたのが1回目。そして今回、再度同じエラーに遭遇した時に最初の反射が「`.cjs`にリネームしよう」だった。ユーザーの「importで書くべきでは？」は完全に正しい。プロジェクトがESMなのだからESMで書くのが当然で、拡張子ワークアラウンドは対症療法でしかない。

1回目のミスは「環境確認不足」で説明がつく。2回目は「前回の反省を活かせていない」ので質が悪い。短時間で同じ判断ミスを繰り返すのは、教訓を記憶として定着させるプロセスに問題がある。

### 良かったこと

PreCompact hook → cc-log-to-md.js → diary-cc-logs/ → write-diaryスキル、という一連のパイプラインが実際に動くことを確認できた。今まさにこの日記を書く際に`diary-cc-logs/20260304123234.md`を読んで、圧縮前の会話内容を把握した上で書いている。仕組みとして機能している。
