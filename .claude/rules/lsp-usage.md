# LSP活用ルール

TypeScript中心のリポジトリなので、型・シンボル解決が絡む調査は LSP を優先する（Grep+Read より確実かつ速い）。

## 注意事項

- **コールドスタート時は `any` や "No hover information" が返ることがある**: インデックス未完了の可能性。同じ位置にもう一度呼び出すか、別ファイルを1回触ってから再試行する
- **line/character は1-based**: エディタ表示と同じ
- **character位置はシンボル本体に合わせる**: 空白・記号上では情報が返らない
