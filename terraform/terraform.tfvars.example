# ローカル実行用のサンプル。`terraform.tfvars` にコピーして使う（.gitignore 済み）。
# CI では GitHub Secrets/Vars から TF_VAR_* 経由で渡すため不要。

cloudflare_account_id = "your-cloudflare-account-id"
environment           = "stg"

# cloudflare_api_token は tfvars に書かず環境変数で渡す:
#   export TF_VAR_cloudflare_api_token="..."
